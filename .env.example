# Signs Payload's auth tokens and encrypts stored credentials. Any long random
# string will do — generate one with `openssl rand -hex 32`.
PAYLOAD_SECRET=

# Postgres connection string. Neon provides it (#31); until then Payload boots
# but every request that touches the database fails, including `/cms`.
DATABASE_URL=
