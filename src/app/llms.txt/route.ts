// A handler rather than a file in `public/`, so the links are absolute from
// `METADATA_BASE_URL` and the pitch is the app constant, not a second copy.
export { llmsTextResponse as GET } from '@/shared/lib/llmsTextResponse';
